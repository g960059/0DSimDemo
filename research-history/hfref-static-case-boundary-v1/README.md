# HFrEF static-case production boundary and control audit

`evidence.tar.gz` preserves REPORT section20, the unchanged candidate's renewed
source-bound qualification, six selected controls and four follow-up conditions,
test reports, the assembled case-evidence record, and the local browser audit.
The preceding construction/runtime reviews remain in their earlier archives.

Production72 source/codec/fitting identities and its original52-control domain
are restored without modifying released checkpoints/artifacts. The enlarged
geometry and53-control domain remain isolated research implementations. Obsolete
non-dilated HFrEF source is recoverable from Git through `a20316ed`.

The resting candidate is unchanged. Actual control continuation succeeded for
the selected LV tension, TBV and HR endpoints. TBV7000 cold initialization failed
at2ms and passed at1ms; that limit is retained, not relabeled as nominal-grid
whole-domain qualification. HR60's original failure was an HR70-only observation
harness, corrected in a separate diagnostic without changing the case reference.

Local browser testing found a React DEV Performance Tracks memory failure.
A production-React, local-only preview restores visual inspection; the normal
DEV issue is not claimed permanently fixed. Its configuration is archived, not
the generated application distribution. No new model mint, public registration,
or unconditional formal-adoption review vote is represented by this archive.

The repository hygiene audit also caught machine-local checkout paths in an
older archived review brief. Only those locations were made portable, with an
editorial note; the original prompt remains in Git and no verdict was changed.

Extract into an empty directory. Each run's `execution.source.json` binds its
source archive and outputs; nested source snapshots omit installed dependencies.
Do not use this research archive as a deploy bundle. `CHECKSUM.sha256` records
the outer archive digest. Completed research belongs here/Git, not the hot path.
