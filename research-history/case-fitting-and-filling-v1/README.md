# Shared case fitting and filling-phase investigation

Archive SHA-256: `07b62016bc27cbec7b9f6e7e22250ca43ef6fb4ae75b93d2086eb868e24be81e` (68 members).

2026-09-10. `evidence.tar.gz` preserves the completed investigation with paths
relative to `artifacts/case-workflow-v1/`. See its `REPORT.md` for the Japanese
report, measurements, primary literature, failed attempts and remaining work.

Baseline and the selected HFrEF case now share lean periodic rest execution,
with separate reference assessments, source/anatomy-bound continuation and
raw evidence retained when a measurement is unavailable. This is not an
automatic optimizer, final reserve qualification or public preset adoption.

Final runs are `cold-003` and `warm-002`: cold 26.1/61.2 seconds; same-condition
three-cycle rechecks about 2.6 seconds each. The nine-run local batch took
38.2 seconds. Two low-TBV HFrEF runs remain assessment-unavailable, not passed:
tiny TV reopening before PV opening, markedly smaller at 1 ms than at 2 ms.
LA/LV passive perturbations and phase-resolved pressure/flow are retained.
No parameter choice, equation, physiological gate or range was changed.

`evidence-audit.json` verifies every run's source archive and recorded outputs.
The earlier failed result handling is preserved in `warm-001`, not relabeled.
The report also separates the pre-existing Standard72 source/release mismatch
from full validation of its retained artifact and admission lock. The latter
cannot rebuild, reseal, publish or approve a successor.

Focused canonical 41 tests, fast 1187 tests, PR smoke 875 tests, typecheck and
production build passed. No full canonical/nightly or new browser audit is
claimed. Formal successor registration still needs a coherent qualified
release/case/Surface/document set and the agreed adoption review.
The excluded case experiment/article was not created. No credentials,
paper PDFs, supplied screenshots or installed dependencies are archived.
