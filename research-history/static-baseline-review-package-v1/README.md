# Own baseline qualification and coherent static-case review package

2026-09-10. Implementation: `192b74b7`. `evidence.tar.gz` preserves paths
relative to `artifacts/static-case-package-v1/`. Its Japanese `REPORT.md`
contains the measurements, failed attempts, source reuse and remaining scope.
The final local package is `review-002`, not the intermediate `review-001`.

Archive SHA-256: `8a01644322265cee9e2043d7a963d3749e090aa88ab4d3b5e9dbe583b5c1433b`
(64,223,639 bytes). Archive regular-file paths match the source directory.

The static exact owner's baseline passed independent cold 2/1 ms rest,
relaxation, paired pressure-rate and existing fixed-control low/high preload
checks in 65.781 s with two local workers. No equation, parameter, physiology
threshold, afterload test or optimizer change. Baseline launches from its own
qualified checkpoint after an actual tiny step to the presentation boundary;
the qualified complete beat is retained. The selected HFrEF capture is unchanged.
Its historical baseline comparison is preserved rather than rewritten.

The exact artifact remains byte-identical to the previously reviewed case:
`389640585757cba94eb7126ac055c8026386fef3f7f25fb23c182f939f3a0853`.
Both Chromium and WebKit ran the ordinary simulation Worker with the new
baseline capture and existing HFrEF. Each completed PV and responsive Starling
low-volume analyses, normal save/restore, and 1000-step continuation checks.
`current-bits-final` binds the final source to the same artifact, Worker bytes
and Surface; this is proof reuse, not another browser run.

The new baseline document uses shared explanation/equation templates and own
resolved inputs and observations. The HFrEF v4 scientific document is unchanged.
The final package binds case, document, construction, artifact and checkpoint
identities. Its manifest SHA-256 is
`80559e9233a112805b481c0054b9179f5cd86ca5ddeeaf7a2196fc6273b72cf6`.
Japanese/English offline document checks at 1280/390 px passed with details
closed/open, 88 metric rows, no horizontal overflow or KaTeX errors.

Focused 49, fast 1187 and PR smoke 875 tests, typecheck, production build and
repository hygiene passed. Suites overlap; full canonical/nightly were not run.
The archive retains intermediate failed drivers/tests and draft documents.
Source snapshots cover each numerical run; later reassessment does not rewrite
their execution provenance. No credentials, supplied images, paper PDFs or
installed dependencies are included.

This remains an unregistered research package. No new independent review,
numbered mint, active-default change, publication or authored case
experiment/article. Previously scoped scientific/document approval is not
public-package approval. Freeze and bind the final exact/Surface/case/document
package, then obtain the agreed batched 1/2 adoption review before release.
The earlier low-TBV inlet-event and disease-scope limitations still apply.
