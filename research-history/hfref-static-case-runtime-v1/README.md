# Static-anatomy HFrEF runtime and analysis qualification

2026-09-09. Archive SHA-256:
`9c935cfefe3b56195573ab30367ee410411fc54c8a66eac3bbea50d02188513c`.
`evidence.tar.gz` has 67 members, with paths relative to
`artifacts/hfref-domain-v1/`.

- `REPORT.md`: full research chronology, including this phase's measurements,
  definitions, numerical limits, browser findings and remaining adoption work.
- `static-case-lab-003/`: three independent cold runs (baseline 2 ms, enlarged
  HFrEF 2 and 1 ms), three-cycle P1 plus a withheld audit, compiled/source
  baseline → case → baseline exact route parity, JSON continuation, local
  controls and eight formal/responsive PV/Starling partitions. All completed
  in 179.126 seconds wall time. Individual source/result hashes were verified.
  Reviewed source SHA-256:
  `39e55c559a267d887c0089dca317d422e9998e228bb73461da5df67afb0fbff1`.
- `static-case-lab-smoke-001/`: successful two-cycle smoke, not settlement.
  `static-case-lab-001/`: interrupted driver/import attempt without a final
  source seal. `static-case-lab-002/`: retained failure from reusing a retired
  runtime ID. These were harness issues, not physiological nonconvergence.
- `static-case-slow-tail-001/`: post-review 60-cycle extensions of both grids.
  Coronary-tone increments decreased monotonically without reversal, while
  CI changed about 0.00046 L/min/m² and mean Ao about −0.013 mmHg. The tone
  itself continued changing; a fully stationary plateau is not claimed. The
  extension did not replace the local launch checkpoint or change a gate.
- `static-case-runtime-review/`: common unopinionated brief, complete GPT-6
  Astra xhigh and Claude Fable 5.1 xhigh reviews, actual Fable result metadata,
  main-agent browser audit, slow-tail measurement script and adjudication.
  **Local scoped implementation: 2/2 APPROVE. Formal public adoption: both
  CONDITIONAL, zero unconditional votes, not adopted.** Test JSON retains the
  final 110 passing tests; typecheck and diff checks also passed.

The local launch is the 2 ms capture; 1 ms provides sensitivity evidence.
Static anatomy is not evolving remodeling, ischemia, or a generic HFrEF
phenotype. Actual tissue occupies the unchanged pericardial bag; the coronary
reference bed/demand remains separate from current mass. Mass-aware MVO2 is a
versioned illustrative literature estimate, not validated disease metabolism.
Its unknown contractility-dependent intercept and the PV family's extension
limits remain visible. The full inherited control domain has not been
qualified for enlarged anatomy by local control edits alone.

The next adoption work is source/release identity coherence, bounded control
qualification and an explicit final case/documentation/registration record.
Earlier production72/research checkpoint, registry and fitting-workflow
boundary failures remain; no green full PR/canonical lane is claimed. Do not
fix those by relabeling checkpoints or weakening identity checks.

The accompanying code is the local static-case host, inherited Surface and
mass-aware analysis; no numbered public model mint or public preset promotion
is implied. Prior anatomy/mechanical controls remain in earlier archives.
No paper PDFs, supplied book screenshots, credentials, or dependencies are
included. Later cleanup/adoption belongs to subsequent commits, not edits to
this sealed archive.
