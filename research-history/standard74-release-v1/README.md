# Standard74 adoption evidence

2026-09-16. New exact identity; latest compatible production Model Surface
and versioned analysis methods inherited. This is not a Standard73
bit-equivalent artifact replacement or a physiological refit.

`evidence.tar.gz` SHA-256:
`013a304bdde48a0e6607cbab27bec07ad67a5defaa30d373035a731dee502406`.
The fixed local package SHA-256 is
`28dfc475c562cce5871934c465e142aa27c1209d04c2a4e54b015e6351213061`.

The archive contains independent cold 2/1 ms qualification and sealed source
snapshots; four own launch dossiers and prepared analyses; final deterministic
artifact and candidate/source/compiled continuation; actual Chromium/WebKit
Worker capture/restore evidence; desktop/mobile/reader UI checks; independent
GPT 6 Astra max and Claude Opus 5 max scientific reviews and code-review
disposition; and local PostgreSQL migration/rollback checks. Final B's four
scientific dossiers received ACCEPT from both reviewers. Final code/merge/CI
decisions are recorded on PR #631, not inferred from those scientific votes.

`worker-evidence.json` binds both browsers to the final exact artifact. The
20 browser checks passed without retries. The candidate→source→compiled
comparison covers 10,000 exercised steps and 28 checkpoints, not the complete
control domain or Standard73 trajectory equivalence. Clinical normality and
general clinical validation are not claimed; case limitations remain in each
self-contained document.

The public transfer receipt records admission of 22 contents / 66 Scenario
captures. Private article bodies, owner identifiers, migration SQL and recovery
backups are intentionally excluded. Compatible authored checkpoints remain
predecessor-derived initial conditions; see the registry tool's operator
boundary for provenance retention and rollback. Publication, active-default
selection and actual database transfer are explicit later operations, not
effects of possessing this archive.

Retired candidate/Standard73 runtime data remain recoverable from Git and the
sealed source snapshots. Historical documents and assessments remain readable
independently of those runtime implementations.
