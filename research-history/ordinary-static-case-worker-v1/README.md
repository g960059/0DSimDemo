# Ordinary Worker audit for baseline and fixed HFrEF

2026-09-10. `evidence.tar.gz` has paths relative to
`artifacts/static-case-worker-audit-v1/`. See its Japanese `REPORT.md` and
`run-006/report.json` for the final browser evidence, and the archive hash below.

Archive SHA-256: `03160db7daa5a963e7e9a6cac867c20216443accdca568bfd5311348339e1e99`
(72 members; 82,656,515 bytes).

Chromium and WebKit executed the actual `StudioSimulationWorkerV2`, dynamic
artifact loader, pinned analyses and ordinary capture/save/restore protocol.
Baseline and the selected HFrEF use their own anatomy and settled checkpoint;
incompatible anatomy/capture input is rejected without altering existing state.
Both cases completed formal PV and responsive Starling low-volume partitions.
Two-case restore preserves full numerical captures, and each case matches for
1000 continued steps, including selected samples and full terminal frames.

Restore-initial ephemeral outputs may be explicitly not-evaluated, as the exact
checkpoint contract requires; these IDs are recorded, not silently ignored.
All other initial outputs match, the first advanced full frame matches, and
final captures match. This is not cross-browser bit identity, full UI visual
QA, qualification of every control combination, or new physiological admission.

The exact artifact is byte-identical to previously reviewed static-case-lab-004:
`389640585757cba94eb7126ac055c8026386fef3f7f25fb23c182f939f3a0853`.
Worker hash: `3b70dccb3397510c753af7393de4b4cb9b5c163fc1fb00be4ddc1313114eb0ae`.
`current-bits-final` rebuilds both from the final observation-window-fix source
and verifies the same bytes and Surface. This reuses the recorded browser proof
by byte identity; it is not an additional browser run. The full run took 156.4 s.

Failed harness attempts are preserved with their corrections. The companion
`inlet-event-and-fitting-window-v1` archive contains the run-integrity audit.
Existing scientific approval and public-package adoption remain separate.
No new review, model mint, publication, server writes or authored case
experiment/article. All browser requests were served from known local bytes;
no credentials, supplied screenshots, papers or installed dependencies included.
