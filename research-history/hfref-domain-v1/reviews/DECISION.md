# Consolidated range/exposure review decision

Both requested reviewers completed one independent round: GPT-6 Astra xhigh
and Claude Fable 5.1 xhigh. Both conditionally approve bounded LVFW+SEP active
amplitude extension and a first development demonstration. No additional vote
is required merely to complete the stated implementation tests. Neither approves
publication or an arbitrary future disease domain.

I adopt Astra's concrete conditions as the 1-of-2 acceptance gate: LVFW/SEP
.25–1.33; RVFW/atria/global ventricular group .75–1.33; one atomic group operation;
released72 isolation; fresh own-model checkpoint; baseline→.35→1 capture/reload
and periodic output concordance; lower-bound and transaction checks; inherited
analysis/Surface continuity; explicit disease and measurement limitations.
Implementation remains an isolated research worktree until those checks finish.

Shared findings accepted: no added calcium kinetics or remodeling just to cross
the optional EDVI preference; expose and test a real baseline-to-preset operation;
avoid averaging a mixed wall state; preserve non-LV inputs; distinguish canonical
fixture equality from history-dependent checkpoint equality; report congestion,
RV interaction, timing direction caveat and unresolved Glantz fit. The .35 demo
is not the numerical optimizer's winner and is not all chronic HFrEF.

Clarifications / recommendations not adopted literally:

- A new exposed operation needs an inherited but separately identified Surface;
  Claude's suggestion to keep the Surface unchanged would leave it unexposed.
- Claude described the runtime as a fixed Klotz passive law. The current exact
  construction uses five-wall constitutive mechanics; a Klotz reference used in
  prior construction is not a replacement for that runtime law. Fixed geometry
  is a limitation, but this review does not establish unique geometry causality.
- The prior active1 cold case was not a tested reversal. Both reviewers noticed
  the warm diagnostic began at .30. New actual adapter routes are required.
- Exhaustive Cartesian validity is not claimed. Use bounded endpoint/transaction
  checks and the actual route, not a new open-ended safety-envelope project.
  Astra's numerical tolerances are comparison limits, not new clinical gates.
- Patel Table2 agrees with the registry but the abstract reverses the two group
  relaxation medians. Independently checked publisher HTML; add the discrepancy
  note, not a silent number correction. Old reference/result hashes stay intact;
  the metadata annotation creates a new reference-content hash. No numeric
  screen, fitting target or source measurement was changed.

The released implementation worktree passed its existing Standard72 studio
binding suite (12 tests, including controls, checkpoint continuation, PV and
Starling analysis) and the original HFrEF suite (10 tests) during this work.
The parameter/identity prototype is not copied into that worktree. The isolated
research worktree is `codex/hfref-domain-experiment-v1`.

## Implementation follow-through

The selected Astra conditions subsequently passed in route-002 and its gradual
follow-up: forward/reverse settled endpoint parity, JSON reload, source/artifact
parity, endpoints, individual-wall lower bounds and malformed/stale operations.
Real-browser checks confirmed the grouped control, preset selection, PV curves
and shared Starling family. A transient-source analysis failure remains an
explicit failure (no gate relaxation); the new error-detail retry successfully
recomputed the same pinned analysis after settlement. The range and grouped
operation are accepted for this isolated development model. This does not
authorize arbitrary future ranges, clinical validation, or public promotion.
