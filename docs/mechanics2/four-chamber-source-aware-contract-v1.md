# Four-Chamber Source-Aware Contract V1

This diagnostic applies source-aware status ownership as a contract layer over
the selected four-chamber residual review. It does not change four-chamber
dynamics.

Report:

- `data/mechanics2/reports/four-chamber-source-aware-contract-report-v1.json`

Result:

- Decision: `source-aware-four-chamber-contract-signal`
- Raw reference: 17/21.
- Left source-aware status: 18/21.
- Left plus right-preload source-aware status: 19/21.
- Left plus right source-aware status with clean low-output phenotype scope:
  20/21.
- Remaining blocker: `long-epochs/preload-low` with
  `persistent-large-reservoir-shuttle`.

Interpretation:

- Phase-aligned left source parity and load-conditioned output reserve can be
  represented at the contract layer.
- The right preload-low PV outflow lead can be treated as source ownership for
  classification, but direct valve-parameter transfer remains blocked by the
  previous review.
- Clean low-output phenotype scope must be explicit; applying it resolves the
  `dt-half/contractility-low` status blocker only as a source-aware policy
  signal.
- The remaining blocker is long-epoch reservoir repeatability, not AV-plane or
  LandAtrial.

Claim boundary:

- This is not runtime wiring, true four-chamber dynamics, morphology
  acceptance, reservoir retuning, AV-plane work, or LandAtrial work.
- Next work should promote the source-aware status contract into a bounded
  four-chamber contract smoke and then address the remaining reservoir
  repeatability blocker.
